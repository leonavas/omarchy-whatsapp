{
  "schemaVersion": 1,
  "id": "leonavas.whatsapp",
  "name": "WhatsApp",
  "version": "1.1.0",
  "author": "leonavas",
  "license": "MIT",
  "description": "A tray-style WhatsApp button for the bar: opens the web app (or the bundled shell app), parks it out of sight, carries the unread count as a badge, and previews unread chats on hover.",
  "kinds": [
    "bar-widget"
  ],
  "entryPoints": {
    "barWidget": "WhatsApp.qml"
  },
  "barWidget": {
    "displayName": "WhatsApp",
    "description": "WhatsApp as a tray icon: click to show or hide the window, with a badge while there are unread chats.",
    "category": "Status",
    "aliases": [
      "whatsapp",
      "zap",
      "chat",
      "messages",
      "tray"
    ],
    "allowMultiple": false,
    "defaultSection": "right",
    "defaults": {
      "launchCommand": "",
      "windowClass": "whatsapp",
      "iconStyle": "Glyph",
      "glyph": "󰖣",
      "glyphSize": 0,
      "glyphOffsetY": -0.5,
      "badge": "Dot",
      "badgeColor": "",
      "groupColor": "#3b82f6",
      "badgeScale": 0.45,
      "tintWhenUnread": false,
      "hideMode": "Special workspace",
      "specialWorkspace": "whatsapp",
      "startHidden": true,
      "hideAfterLaunch": 8,
      "autoStart": false,
      "dimWhenClosed": true,
      "hideWhenNotRunning": false,
      "middleClickCloses": true,
      "hoverPreview": true,
      "previewRows": 6
    },
    "schema": [
      {
        "key": "launchCommand",
        "type": "string",
        "label": "Custom launch command",
        "defaultValue": "",
        "description": "Left empty, the widget launches the bundled shell app (shell/whatsapp-shell), which is what feeds the hover preview. Set it to run something else, e.g. `omarchy-launch-webapp https://web.whatsapp.com/` or `whatsapp-for-linux`."
      },
      {
        "key": "windowClass",
        "type": "string",
        "label": "Window class to watch",
        "defaultValue": "whatsapp",
        "description": "Matched as a case-insensitive substring of the window class. \"whatsapp\" covers the browser web app (chrome-web.whatsapp.com__-Default) and native clients alike."
      },
      {
        "key": "iconStyle",
        "type": "enum",
        "label": "Icon",
        "options": [
          "Glyph",
          "App icon"
        ],
        "defaultValue": "Glyph",
        "description": "Glyph draws the Nerd Font logo in the bar's own color; App icon draws the green WhatsApp icon from the icon theme."
      },
      {
        "key": "glyph",
        "type": "string",
        "label": "Glyph",
        "defaultValue": "󰖣",
        "description": "Only used by the Glyph style. Any Nerd Font character works."
      },
      {
        "key": "glyphSize",
        "type": "integer",
        "label": "Glyph size (px)",
        "min": 0,
        "max": 32,
        "step": 1,
        "defaultValue": 0,
        "description": "0 follows the bar's icon size — which is what makes this outline glyph's ink box match the solid icons around it. Only used by the Glyph style."
      },
      {
        "key": "glyphOffsetY",
        "type": "number",
        "label": "Vertical nudge (px)",
        "min": -6,
        "max": 6,
        "step": 0.5,
        "defaultValue": -0.5,
        "description": "Negative moves the glyph up. The bar lines glyphs up by the font's line box rather than by their ink, so an icon glyph with unusual metrics needs a nudge to sit on the same line as its neighbours."
      },
      {
        "key": "badge",
        "type": "enum",
        "label": "Unread badge",
        "options": [
          "Count",
          "Dot",
          "None"
        ],
        "defaultValue": "Dot",
        "description": "Count writes the number of unread chats over the icon, Dot only marks that there is something new."
      },
      {
        "key": "badgeColor",
        "type": "string",
        "label": "Badge color (one-to-one chats)",
        "defaultValue": "",
        "description": "A hex color such as #25d366, used while a conversation with a single person is waiting. Empty follows the theme's urgent color — red in most themes — like the rest of the bar."
      },
      {
        "key": "groupColor",
        "type": "string",
        "label": "Badge color (groups)",
        "defaultValue": "#3b82f6",
        "description": "The color the badge takes when only groups are unread, so the bar tells a group message from a personal one at a glance. A mix of both paints the one-to-one color: the badge shows the most urgent thing waiting. Empty uses the same color for both. Needs the bundled shell app — the plain browser web app only publishes a count."
      },
      {
        "key": "badgeScale",
        "type": "number",
        "label": "Dot size",
        "min": 0.2,
        "max": 0.9,
        "step": 0.05,
        "defaultValue": 0.45,
        "description": "The dot's diameter as a fraction of the icon, the same scale the tray daemon paints by. 0.45 reads like Slack's; 0.6 is the tray daemon's own default."
      },
      {
        "key": "tintWhenUnread",
        "type": "boolean",
        "label": "Color the icon while there are unread messages",
        "defaultValue": false
      },
      {
        "key": "hideMode",
        "type": "enum",
        "label": "What a click on the open window does",
        "options": [
          "Special workspace",
          "Never hide"
        ],
        "defaultValue": "Special workspace",
        "description": "Special workspace parks the window in a hidden Hyprland special workspace — it keeps running and keeps counting, like an app minimized to the tray."
      },
      {
        "key": "specialWorkspace",
        "type": "string",
        "label": "Name of the special workspace",
        "defaultValue": "whatsapp",
        "description": "Used as special:<name>."
      },
      {
        "key": "startHidden",
        "type": "boolean",
        "label": "Park the window right after opening it",
        "defaultValue": true,
        "description": "The window loads, then disappears into the special workspace on its own, leaving only the bar icon."
      },
      {
        "key": "hideAfterLaunch",
        "type": "integer",
        "label": "Seconds before parking it (s)",
        "min": 0,
        "max": 60,
        "step": 1,
        "defaultValue": 8,
        "description": "Time for the page to load before it is put away. Too short and WhatsApp may never finish loading."
      },
      {
        "key": "autoStart",
        "type": "boolean",
        "label": "Open WhatsApp when the shell starts",
        "defaultValue": false,
        "description": "The icon's menu toggles this as \"Launch on Login\". With \"Park the window right after opening it\", WhatsApp comes up on login already out of the way, counting messages in the bar."
      },
      {
        "key": "dimWhenClosed",
        "type": "boolean",
        "label": "Dim the icon while WhatsApp is not running",
        "defaultValue": true
      },
      {
        "key": "hideWhenNotRunning",
        "type": "boolean",
        "label": "Remove the icon from the bar while WhatsApp is not running",
        "defaultValue": false,
        "description": "Off by default: with the icon always there, the bar keeps the button that opens WhatsApp."
      },
      {
        "key": "middleClickCloses",
        "type": "boolean",
        "label": "Middle click closes WhatsApp",
        "defaultValue": true
      },
      {
        "key": "hoverPreview",
        "type": "boolean",
        "label": "Preview unread chats on hover",
        "defaultValue": true,
        "description": "Hovering the icon opens a popup with contact, last message and count per unread chat. Needs WhatsApp running in the bundled shell app (shell/whatsapp-shell) — the plain web app has no way to hand the messages out."
      },
      {
        "key": "previewRows",
        "type": "integer",
        "label": "Chats shown in the preview",
        "min": 1,
        "max": 20,
        "step": 1,
        "defaultValue": 6
      }
    ]
  }
}
